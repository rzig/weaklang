f dim(list) {
    r (s (s list))[0];
}

f funky_multiply(list) {
    v dim(list) == 1;
    a len = (s list)[0];
    a j = 0;
    w (j < len) {
        list[j] = len * list[j];
        j = j + 1;
    }
    r list;
}

a arr = [1, 2, 3];
p funky_multiply(arr);
# p arr;